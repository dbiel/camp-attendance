rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (simple camp app, no auth)
    // For production, consider adding authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
